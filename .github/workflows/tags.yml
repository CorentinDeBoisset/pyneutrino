name: Tags CI

concurrency:
  group: '${{ github.workflow }} @ ${{ github.ref }}'
  cancel-in-progress: true

on:
  push:
    tags:
      - '[0-9]\.[0-9]\.[0-9]*'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - run: echo "TODO BUILD DOCKER"
